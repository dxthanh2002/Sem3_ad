@model IEnumerable<WebApplication1.Models.AgesModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.agenumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.agenumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.agesid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.agesid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.agesid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
